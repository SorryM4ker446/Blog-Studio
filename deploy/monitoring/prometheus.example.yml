global:
  scrape_interval: 30s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/rules/blog-studio.rules.yml

scrape_configs:
  - job_name: blog-studio-backend
    metrics_path: /internal/metrics
    static_configs:
      - targets:
          - backend:8080
